# Pico Bot — Python package dependencies
# Install with: pip3 install --break-system-packages -r requirements.txt
# Generated from OpenClawPI (pip3 freeze, project-relevant packages only)
# Last updated: 2026-03-15

# Telegram bot
pyTelegramBotAPI

# Voice assistant — Speech-to-Text
vosk
sounddevice
webrtcvad          # optional — VAD pre-filter; graceful fallback if absent

# Gmail digest — Google API
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Web UI (FastAPI)
fastapi
uvicorn[standard]
jinja2
bcrypt
PyJWT
python-multipart
requests               # Google OAuth token refresh + HTTP calls (bot_web.py)

# Storage — PostgreSQL + pgvector (optional; required for STORE_BACKEND=postgres)
psycopg[binary]
psycopg-pool
pgvector

# Documents / RAG
sentence-transformers  # all-MiniLM-L6-v2 embeddings
pdfminer.six           # PDF text extraction
python-docx            # .docx text extraction
sqlite-vec             # SQLite vector search extension (RAG / KNN embeddings)
cryptography           # optional dependency for psycopg TLS
