# MCP client dependencies (used in bot_mcp_client.py to speak MCP protocol to N8N)
# The server side is N8N (MCP Server Trigger) -- no FastAPI/uvicorn needed here.
mcp>=1.2.0
httpx>=0.27.0
httpx-sse>=0.4.0
pydantic>=2.7.0
