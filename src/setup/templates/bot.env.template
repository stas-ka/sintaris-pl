# bot.env — generated by deploy script from .env + this template
# DO NOT edit this file manually; edit .env and regenerate.
# Generated: see src/setup/taris_deploy.sh or src/setup/gen_config.sh

# Telegram bot credentials
BOT_TOKEN=${BOT_TOKEN}
ALLOWED_USERS=${ALLOWED_USERS}

# Device variant (openclaw / taris / picoclaw)
DEVICE_VARIANT=${DEVICE_VARIANT}

# LLM providers
LLM_PROVIDER=${LLM_PROVIDER}
LLM_FALLBACK_PROVIDER=${LLM_FALLBACK_PROVIDER}
OLLAMA_MODEL=${OLLAMA_MODEL}
OLLAMA_URL=${OLLAMA_URL}
OLLAMA_THINK=${OLLAMA_THINK:-false}
OPENAI_API_KEY=${OPENAI_API_KEY}

# STT (faster-whisper on OpenClaw)
STT_PROVIDER=${STT_PROVIDER}
FASTER_WHISPER_MODEL=${FASTER_WHISPER_MODEL}
FASTER_WHISPER_DEVICE=${FASTER_WHISPER_DEVICE:-cpu}
FASTER_WHISPER_COMPUTE=${FASTER_WHISPER_COMPUTE:-int8}

# Database (PostgreSQL on VPS)
STORE_BACKEND=${STORE_BACKEND}
POSTGRES_DSN=${POSTGRES_DSN}

# N8N workflow automation
N8N_BASE_URL=${N8N_BASE_URL}
N8N_API_KEY=${N8N_API_KEY}
N8N_CAMPAIGN_SELECT_WH=${N8N_CAMPAIGN_SELECT_WH}
N8N_CAMPAIGN_SEND_WH=${N8N_CAMPAIGN_SEND_WH}
N8N_CREDENTIAL_ID_SMTP=${N8N_CREDENTIAL_ID_SMTP}
N8N_CREDENTIAL_ID_GSHEET=${N8N_CREDENTIAL_ID_GSHEET}
# Google Sheet ID for campaign status log (required — leave empty to use hardcoded default)
CAMPAIGN_SHEET_ID=${CAMPAIGN_SHEET_ID}

# Webhook authentication (outbound + inbound verification)
WEBHOOK_AUTH_TYPE=${WEBHOOK_AUTH_TYPE:-none}
WEBHOOK_AUTH_TOKEN=${WEBHOOK_AUTH_TOKEN}
WEBHOOK_AUTH_HEADER=${WEBHOOK_AUTH_HEADER:-X-Api-Key}
WEBHOOK_HMAC_SECRET=${WEBHOOK_HMAC_SECRET}

# Google Sheets integration
VPS_GSHEET_CLIENTS_ID=${VPS_GSHEET_CLIENTS_ID}

# Web UI
WEB_SECRET_KEY=${WEB_SECRET_KEY}
WEB_PORT=${WEB_PORT:-8080}
