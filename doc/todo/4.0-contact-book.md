# Contact Book — Full Spec

**Status:** 🔲 Planned  
**Module:** `src/bot_contacts.py` (new)  
**Storage:** `pico.db` `contacts` table (see [SQLite spec](9-sqlite-data-layer.md))

---

## Data Schema

```sql
CREATE TABLE IF NOT EXISTS contacts (
    id          TEXT    PRIMARY KEY DEFAULT (lower(hex(randomblob(4)))),
    chat_id     INTEGER NOT NULL REFERENCES users(chat_id),
    name        TEXT    NOT NULL,
    phone       TEXT,
    email       TEXT,
    address     TEXT,
    notes       TEXT,
    created_at  TEXT    DEFAULT (datetime('now')),
    updated_at  TEXT    DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_contacts_chat_name ON contacts(chat_id, name COLLATE NOCASE);
```

---

## Operations

| Operation | Telegram | Voice | Web |
|---|---|---|---|
| **Add** | ➕ button → multi-step ForceReply (name → phone → email → notes) | "добавь контакт Иван Иванов телефон ..."; NL → LLM extracts fields | Form on `/contacts/new` |
| **View** | `contact_open:<id>` button or NL "найди Ивана" | "покажи Ивана" → TTS | `/contacts/<id>` |
| **List** | `contact_list` + inline search | "кто у меня в контактах?" → TTS list | `/contacts` paginated |
| **Update** | `contact_edit:<id>` → ForceReply | "обнови телефон Ивана на ..." | HTMX in-place edit |
| **Delete** | `contact_del:<id>` → confirm → `contact_del_confirm:<id>` | "удали Ивана" + confirm | Confirm modal |
| **Search** | NL or console | "найди контакт с gmail" | `/contacts?q=` |

---

## Callback Keys

| Key / Prefix | Handler |
|---|---|
| `menu_contacts` | `_handle_contacts_menu` |
| `contact_list` | `_handle_contact_list` |
| `contact_create` | `_start_contact_add` |
| `contact_open:<id>` | `_handle_contact_open` |
| `contact_edit:<id>` | `_start_contact_edit` |
| `contact_del:<id>` | `_handle_contact_delete` (confirm card) |
| `contact_del_confirm:<id>` | `_handle_contact_delete_confirmed` |
| `contact_search` | `_handle_contact_search` |
| `contact_page:<n>` | `_handle_contact_list` with offset |

---

## `_user_mode` Values

| Value | Set by | Cleared by |
|---|---|---|
| `"contact_add"` | `_start_contact_add()` | `_finish_contact_add()` on last step |
| `"contact_edit"` | `_start_contact_edit()` | `_handle_contact_edit_input()` |
| `"contact_search"` | `_handle_contact_search()` | after results sent |

---

## Voice NL Parsing

```python
CONTACT_EXTRACT_PROMPT = """
Extract contact details from the text. Return ONLY JSON:
{"name": "...", "phone": "...", "email": "...", "address": "...", "notes": "..."}
Use null for absent fields. Name is required.
Text: {text}
"""
# If name is null → ask user to repeat with name
# All other fields optional — save partial contact
```

---

## Telegram Implementation Checklist

- [ ] Create `src/bot_contacts.py` (imports: `bot_config`, `bot_state`, `bot_instance`, `bot_access`)
- [ ] CRUD helpers: `_contact_add()`, `_contact_get()`, `_contact_update()`, `_contact_delete()`, `_contact_search()`
- [ ] `_contacts_menu_keyboard(chat_id)` — ➕ Add / 🔍 Search / 📋 List / 🔙 Back
- [ ] `_contacts_list_keyboard(chat_id, contacts)` — per-contact open/edit/delete buttons (paginated: max 8/page)
- [ ] `_start_contact_add(chat_id)` — ForceReply multi-step: name → phone → email → address → notes
- [ ] `_finish_contact_add(chat_id, text)` — multi-step via `_pending_contact[chat_id]`
- [ ] `_handle_contact_open(chat_id, cid)` — detail card: name, phone, email, address, notes
- [ ] `_start_contact_edit(chat_id, cid)` — field selector (6 buttons) → targeted ForceReply
- [ ] `_handle_contact_delete(chat_id, cid)` — confirm card
- [ ] `_handle_contact_search(chat_id)` — mode `contact_search` → `_contact_search()` → paginated results
- [ ] NL voice: detect "добавь/найди/удали контакт" intent in `_handle_voice_message()` → route to contacts
- [ ] Add `📇 Contacts` button to `_menu_keyboard()` in `bot_access.py`
- [ ] Register all `contact_*` callbacks in `telegram_menu_bot.py`
- [ ] Add all contact UI keys to `src/strings.json` (ru/en/de)
- [ ] Add `contacts` table to `bot_db.py` `init_db()`

## Web UI Implementation Checklist

- [ ] Add routes to `src/bot_web.py`: `GET/POST /contacts`, `GET /contacts/new`, `GET/PUT/DELETE /contacts/{id}`
- [ ] Create `src/templates/contacts.html` — list + search bar + "New contact" button
- [ ] Create `src/templates/_contact_editor.html` — HTMX partial: create/edit form
- [ ] Add 📇 Contacts link to `base.html` nav and `dashboard.html`
- [ ] Voice-fill: record on contacts page → `/api/voice/transcribe` → client-side form fill
